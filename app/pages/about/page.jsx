import AboutSection from '@/components/AboutSection'
import React from 'react'

const page = () => {
  return (
    <div>
      < AboutSection/>
    </div>
  )
}

export default page

export const metadata = {
  title: 'The Vision Academy : About',
};